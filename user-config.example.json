{
  "maxPositions": 3,
  "minFeeActiveTvlRatio": 0.05,
  "minTvl": 10000,
  "maxTvl": 150000,
  "minVolume": 500,
  "minOrganic": 60,
  "minHolders": 500,
  "minMcap": 150000,
  "maxMcap": 10000000,
  "minBinStep": 80,
  "maxBinStep": 125,
  "timeframe": "5m",
  "category": "trending",
  "minTokenFeesSol": 30,
  "maxBundlePct": 30,
  "maxBotHoldersPct": 30,
  "maxTop10Pct": 60,
  "blockedLaunchpads": [],
  "allowedLaunchpads": [],
  "minTokenAgeHours": null,
  "maxTokenAgeHours": null,
  "athFilterPct": null,
  "maxVolatility": 10,
  "minClaimAmount": 5,
  "autoSwapAfterClaim": false,
  "outOfRangeBinsToClose": 10,
  "outOfRangeWaitMinutes": 30,
  "oorCooldownTriggerCount": 3,
  "oorCooldownHours": 12,
  "stopLossPct": -20,
  "minAgeBeforeSL": 15,
  "takeProfitFeePct": 5,
  "minFeePerTvl24h": 7,
  "minAgeBeforeYieldCheck": 60,
  "minSolToOpen": 0.55,
  "deployAmountSol": 0.5,
  "gasReserve": 0.2,
  "trailingTakeProfit": true,
  "trailingTriggerPct": 3,
  "trailingDropPct": 1.5,
  "pnlSanityMaxDiffPct": 5,
  "solMode": false,
  "strategy": "bid_ask",
  "volatilityStrategyThreshold": 2.5,
  "targetDownsidePct": 0.35,
  "targetUpsidePct": 0.2,
  "managementIntervalMin": 10,
  "screeningIntervalMin": 30,
  "healthCheckIntervalMin": 60,
  "temperature": 0.373,
  "maxTokens": 4096,
  "maxSteps": 20,
  "managementModel": "openrouter/healer-alpha",
  "screeningModel": "openrouter/hunter-alpha",
  "generalModel": "openrouter/healer-alpha",
  "darwin": {
    "enabled": false
  },
  "preset": "custom",
  "rpcUrl": "https://mainnet.helius-rpc.com/?api-key=YOUR_KEY",
  "llmProvider": "openrouter",
  "llmBaseUrl": "",
  "llmModel": "openrouter/hunter-alpha",
  "llmApiKey": "",
  "telegramChatId": "",
  "hiveMindUrl": "",
  "hiveMindApiKey": "",
  "dryRun": true,
  "_help": {
    "maxPositions": "Max concurrent open LP positions.",
    "minFeeActiveTvlRatio": "Min fee/active-TVL ratio (%), measured over `timeframe`. Scale this with the timeframe.",
    "minTvl": "Min pool TVL (USD).",
    "maxTvl": "Max pool TVL (USD).",
    "minVolume": "Min pool volume (USD) over `timeframe`. Scale this with the timeframe.",
    "minOrganic": "Min Jupiter organic score (0-100).",
    "minHolders": "Min base-token holder count.",
    "minMcap": "Min base-token market cap (USD).",
    "maxMcap": "Max base-token market cap (USD).",
    "minBinStep": "Min DLMM bin step allowed for deploy.",
    "maxBinStep": "Max DLMM bin step allowed for deploy.",
    "timeframe": "Screening window: 5m|15m|1h|2h|4h|24h. Pool metrics (volume, fee/TVL, volatility) are measured over this window.",
    "category": "Meteora pool-discovery category filter (e.g. trending, top).",
    "minTokenFeesSol": "Min all-time global fees paid by the token (SOL). Below this = likely bundled/scam. Hard rule.",
    "maxBundlePct": "Max bundle-holding % (OKX). Context only, NOT a hard filter.",
    "maxBotHoldersPct": "Max bot-holder % (Jupiter audit). Hard filter.",
    "maxTop10Pct": "Max top-10 holder concentration %. Hard filter.",
    "blockedLaunchpads": "Launchpads to never deploy into, e.g. [\"pump.fun\", \"letsbonk.fun\"].",
    "allowedLaunchpads": "If non-empty, only deploy into these launchpads.",
    "minTokenAgeHours": "Min token age in hours (null = no minimum).",
    "maxTokenAgeHours": "Max token age in hours (null = no maximum).",
    "athFilterPct": "Only deploy if price is >= this % below ATH, e.g. -20 (null = off).",
    "maxVolatility": "Max pool volatility at screening time (~0-5 typical, 5+ high). Hard filter + auto-evolved.",
    "minClaimAmount": "Min unclaimed fees (USD) before claiming.",
    "autoSwapAfterClaim": "Auto-swap claimed base token back to SOL after a standalone claim.",
    "outOfRangeBinsToClose": "If active bin is this many bins ABOVE range, close (pumped far above).",
    "outOfRangeWaitMinutes": "Minutes out-of-range before closing.",
    "oorCooldownTriggerCount": "OOR closes within the window before a pool/token goes on cooldown.",
    "oorCooldownHours": "Cooldown duration (hours) after repeated OOR closes.",
    "stopLossPct": "Close if PnL drops to this % (negative).",
    "minAgeBeforeSL": "Minutes before stop-loss can trigger.",
    "takeProfitFeePct": "Static take-profit when PnL reaches this %.",
    "minFeePerTvl24h": "Min fee/TVL (24h, fixed window) before closing for low yield.",
    "minAgeBeforeYieldCheck": "Minutes before the low-yield close rule can trigger.",
    "minSolToOpen": "Min wallet SOL before opening a new position.",
    "deployAmountSol": "Fixed SOL deployed per new position.",
    "gasReserve": "SOL kept aside for gas.",
    "trailingTakeProfit": "Enable trailing take-profit.",
    "trailingTriggerPct": "Activate trailing once PnL reaches this %.",
    "trailingDropPct": "Close when PnL drops this % from the confirmed peak.",
    "pnlSanityMaxDiffPct": "Max diff between reported and derived PnL % before ignoring a tick.",
    "solMode": "Report positions/PnL/balances in SOL instead of USD.",
    "strategy": "Fallback LP strategy when volatility isn't provided (manual deploys): bid_ask|spot.",
    "volatilityStrategyThreshold": "Pool volatility >= this => bid_ask (high-vol, OOR-resilient, accumulate dips); below => spot (two-sided fee farming). Drives strategy deterministically on autonomous deploys.",
    "targetDownsidePct": "Bin range covers this % price drop below the active bin.",
    "targetUpsidePct": "Bin range covers this % price rise above the active bin (spot only).",
    "managementIntervalMin": "Management cycle frequency (minutes).",
    "screeningIntervalMin": "Screening cycle frequency (minutes).",
    "healthCheckIntervalMin": "Portfolio health-check frequency (minutes).",
    "temperature": "LLM sampling temperature.",
    "maxTokens": "Max output tokens per LLM call.",
    "maxSteps": "Max ReAct steps per agent run.",
    "managementModel": "Model for management cycles (falls back to env LLM_MODEL).",
    "screeningModel": "Model for screening cycles (falls back to env LLM_MODEL).",
    "generalModel": "Model for REPL / chat (falls back to env LLM_MODEL).",
    "darwin": "Adaptive signal-weighting (off by default). Set { \"enabled\": true } to turn on; signal-weights.js reads windowDays/minSamples/boostFactor/decayFactor/weightFloor.",
    "_meta": "Generated template. Defaults mirror config-schema.js (single source of truth). Keys are flat; darwin is nested. _help is ignored by the app. Secrets belong in .env."
  }
}
